<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.learningenglish">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Learning English"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        <activity android:name=".HSAnimalActivity"></activity>
        <activity android:name=".HSFruitActivity" />
        <activity android:name=".HSNumberActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".QuizAnimalActivity" />
        <activity android:name=".QuizFruitActivity" />
        <activity android:name=".LearnAnimalActivity" />
        <activity android:name=".LearnFruitActivity" />
        <activity android:name=".QuizNumberActivity" />
        <activity android:name=".QuizActivity" />
        <activity android:name=".LearnNumberActivity" />
        <activity android:name=".LearnActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>